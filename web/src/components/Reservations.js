import React from "react";

const Reservations = () => {
  const pageStyle = {
    backgroundColor: "#0c091f",
    minHeight: "100vh",
    marginLeft: "200px",
    color: "#fff",
  };

  return <div style={pageStyle}>#Reservations</div>;
};

export default Reservations;
