import React from "react";

const Statistics = () => {
  const pageStyle = {
    backgroundColor: "#0c091f",
    minHeight: "100vh",
    marginLeft: "200px",
    color: "#fff",
  };

  return <div style={pageStyle}>#Statistics</div>;
};

export default Statistics;
